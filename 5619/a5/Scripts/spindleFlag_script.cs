using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class spindleFlag_script : MonoBehaviour
{
    // Start is called before the first frame update
    public bool flag = false;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
